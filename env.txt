# Smartlead Lead Manager — Environment Variables
# ------------------------------------------------
# Paste your API key here to skip the Settings screen in the UI.
# Get it from: https://app.smartlead.ai/app/settings/profile
#
# Leave blank and use the Settings page in the browser instead.

SMARTLEAD_API_KEY=

# Optional: change the port (default 8000)
# PORT=8501
